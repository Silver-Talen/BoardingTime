doctype html
html
    head
        link(href='style.css' rel='stylesheet')
    body
        nav
            ul(class='menu')
                a(href='/' class='menuItem L')
                    li(id='Home') #{'Home'}
                    
                a(href='/create' class='menuItem R')
                    li(id='sign_up') #{'Sign Up'}
                a(href='/login' class='menuItem R')
                    li(id='login') #{'Login'}

                a(id="logout" href='/logout' class='menuItem R')
                    li #{'Logout'}
                a(id="account" href='/account' class='menuItem R')
                    li #{'Account'}                
    img.image(src="/Omega.png" alt="Omega" class='logo')
    block content
    script(src='frontend.js')