extends layout
block content
    head
        title Home
    each item in message
        div(class="messageBoard")
            div(class="msgHead")
                p User: #{item.username}
                p Date: #{item.date}
                div(class="message")
                    p #{item.message}
                a(class="messageButtons" id="edit" name="editButton" + item.username href="/editMessage/" + item.id) Edit
                a(class="messageButtons" id="delete" href="/deleteMessage/" + item.id) Delete
    div(class="messageBoard")
        div(class="msgHead")
            p User: #{username}
            div(class="message")
                form(id='form', method='post', action='/')
                    textarea(name="userPosts", cols="60", rows="10")
                    br
                    input(type='submit', value='Post!', class='signUp button', id='post')
    script().
        var active = '#{session}';
        var userLevel = '#{userLevel}';
        var messageData = '#{message}';
